import { useCallback, useEffect, useRef, useState } from "react";
import { ActivityIndicator, Alert, Animated, Image, Linking, Modal, SafeAreaView, ScrollView, StyleSheet, Text, TextInput, TouchableOpacity, View } from "react-native";
import { router, useLocalSearchParams } from "expo-router";
import { getPublicProfile, PublicProfile } from "../../api/users";
import { sendCharterPaperPlane } from "../../api/bored";
import { reportContent } from "../../api/safety";
import { useAuth } from "../../context/AuthContext";
import { useTheme } from "../../context/ThemeContext";

export default function PublicProfileScreen() {
  const { userId, fromChat } = useLocalSearchParams<{ userId: string; fromChat?: string }>();
  const { token } = useAuth();
  const { colors } = useTheme();
  const [profile, setProfile] = useState<PublicProfile | null>(null);
  const [loading, setLoading] = useState(true);
  const [sending, setSending] = useState(false);
  const [charterDialogOpen, setCharterDialogOpen] = useState(false);
  const [charterNotice, setCharterNotice] = useState<string | null>(null);
  const [charterSent, setCharterSent] = useState(false);
  const [charterMessage, setCharterMessage] = useState("");
  const [activePhotoIndex, setActivePhotoIndex] = useState(0);
  const photoFade = useRef(new Animated.Value(1)).current;

  const load = useCallback(async () => {
    if (!token || !userId) return;
    try { setProfile(await getPublicProfile(token, userId)); }
    catch { Alert.alert("Profile unavailable", "This member is unavailable or has restricted their profile.", [{ text: "Back", onPress: () => router.back() }]); }
    finally { setLoading(false); }
  }, [token, userId]);
  useEffect(() => { load(); }, [load]);
  useEffect(() => { setActivePhotoIndex(0); photoFade.setValue(1); }, [profile?.id, photoFade]);

  function sendCharter() { if (!token || !userId || sending) return; setCharterMessage(""); setCharterNotice(null); setCharterSent(false); setCharterDialogOpen(true); }
  async function send() { const message = charterMessage.trim(); if (!token || !userId || sending || !message) return; try { setSending(true); setCharterNotice(null); const result = await sendCharterPaperPlane(token, userId, message); setCharterSent(true); setCharterNotice(`A red Charter Plane is now on ${profile?.anonymousUsername ?? "their"} desk for 24 hours. ${result.wallet.balance} Paisa remaining.`); } catch (error: any) { setCharterNotice(error?.response?.data?.message ?? "Please try again."); } finally { setSending(false); } }
  async function reportMember() { if (!token || !userId) return; try { await reportContent(token, "USER", userId, "Inappropriate member profile"); Alert.alert("Report received", "Thank you. A moderator will review it."); } catch { Alert.alert("Couldn’t report member", "Please try again."); } }
  function cyclePhoto() {
    if (!profile || profile.photos.length < 2) return;
    Animated.timing(photoFade, { toValue: 0, duration: 140, useNativeDriver: true }).start(() => {
      setActivePhotoIndex((current) => (current + 1) % profile.photos.length);
      Animated.timing(photoFade, { toValue: 1, duration: 220, useNativeDriver: true }).start();
    });
  }


  if (loading) return <SafeAreaView style={[styles.safe, { backgroundColor: colors.canvas }]}><ActivityIndicator style={styles.loader} color={colors.teal} /></SafeAreaView>;
  if (!profile) return null;
  const memberSince = new Intl.DateTimeFormat(undefined, { month: "long", year: "numeric" }).format(new Date(profile.createdAt));
  const activePhoto = profile.photos[activePhotoIndex];
  const hasHiddenPhoto = profile.photoAvailability.total > profile.photoAvailability.visible;
  const hasNoSecondPhoto = profile.photoAvailability.total < 2;
  return <SafeAreaView style={[styles.safe, { backgroundColor: colors.canvas }]}><ScrollView contentContainerStyle={styles.content}>
    <TouchableOpacity onPress={() => router.back()}><Text style={[styles.back, { color: colors.teal }]}>← Back</Text></TouchableOpacity>
    <View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}>
      <View style={styles.identity}><View style={[styles.avatar, { backgroundColor: colors.violetSoft }]}><Text style={[styles.initial, { color: colors.violet }]}>{profile.anonymousUsername.charAt(0).toUpperCase()}</Text></View><View style={styles.identityCopy}><Text style={[styles.name, { color: colors.navy }]}>{profile.anonymousUsername}</Text><Text style={[styles.memberSince, { color: colors.muted }]}>Breakroom member since {memberSince}</Text></View></View>
      {!!profile.limitedProfile && <View style={[styles.limited, { backgroundColor: colors.surfaceSoft }]}><Text style={[styles.limitedText, { color: colors.muted }]}>Only shared photos and public Desk Notes are visible. Other profile details are private.</Text></View>}
      <View style={styles.photoSection}>
        <Text style={[styles.sectionLabel, { color: colors.teal }]}>PROFILE PHOTOS</Text>
        {activePhoto ? <TouchableOpacity activeOpacity={0.9} onPress={cyclePhoto} style={[styles.photoFrame, { borderColor: "#9B6540", shadowColor: colors.navy }]}><View style={styles.photoMat}><Animated.View style={[styles.photoStage, { opacity: photoFade }]}><Image source={{ uri: activePhoto.url }} resizeMode="contain" style={styles.photoImage} /></Animated.View></View>{profile.photos.length > 1 && <View style={styles.photoHint}><Text style={styles.photoHintText}>Tap to see next photo · {activePhotoIndex + 1}/{profile.photos.length}</Text></View>}</TouchableOpacity> : <View style={[styles.photoUnavailable, { borderColor: colors.border, backgroundColor: colors.surfaceSoft }]}><Text style={[styles.unavailableIcon, { color: colors.muted }]}>▧</Text><Text style={[styles.unavailableTitle, { color: colors.text }]}>{profile.photoAvailability.total ? "Photos are not shared in this chat" : "No profile photo available"}</Text><Text style={[styles.unavailableText, { color: colors.muted }]}>This member controls who can see their photos.</Text></View>}
        {activePhoto && <Text style={[styles.photoStatus, { color: colors.muted }]}>{hasHiddenPhoto ? "Another profile photo is private or not shared with you." : hasNoSecondPhoto ? "No second profile photo is available." : "All shared profile photos are available above."}</Text>}
      </View>
      <View style={[styles.detailsPanel, { borderColor: colors.border, backgroundColor: colors.surfaceSoft }]}><Text style={[styles.sectionLabel, { color: colors.teal }]}>PROFILE DETAILS</Text>{!!profile.bio && <Text style={[styles.bio, { color: colors.text }]}>{profile.bio}</Text>}{(!!profile.gender || profile.age !== null) && <View style={styles.tags}>{profile.age !== null && <View style={[styles.tag, { backgroundColor: colors.violetSoft }]}><Text style={[styles.tagText, { color: colors.violet }]}>Age {profile.age}</Text></View>}{!!profile.gender && <View style={[styles.tag, { backgroundColor: colors.tealSoft }]}><Text style={[styles.tagText, { color: colors.teal }]}>{profile.gender}</Text></View>}</View>}{!profile.bio && !profile.gender && profile.age === null && <Text style={[styles.emptyDetails, { color: colors.muted }]}>This member has not shared additional profile details.</Text>}</View>
      {!!profile.socialLink && <TouchableOpacity onPress={() => Linking.openURL(profile.socialLink!)} style={[styles.linkButton, { borderColor: colors.border }]}><Text style={[styles.linkText, { color: colors.violet }]}>Open social profile ↗</Text></TouchableOpacity>}
      {fromChat !== "1" && <TouchableOpacity disabled={sending} onPress={sendCharter} style={[styles.connectButton, styles.charterButton, sending && styles.disabled]}><Text style={styles.connectText}>{sending ? "Sending…" : "Send Charter Plane · 100 Paisa"}</Text><Text style={styles.charterHelp}>Direct delivery · 10× standard cost · 24 hours</Text></TouchableOpacity>}
      <TouchableOpacity onPress={reportMember} style={styles.reportMember}><Text style={[styles.reportMemberText, { color: colors.danger }]}>Report member</Text></TouchableOpacity>
      {!!profile.deskNotes?.length && <View style={[styles.notesSection, { borderColor: colors.border }]}><Text style={[styles.notesTitle, { color: colors.text }]}>Public Desk Notes</Text>{profile.deskNotes.map((note) => <View key={note.id} style={[styles.publicNote, { backgroundColor: colors.amberSoft }]}><Text style={[styles.publicNoteText, { color: colors.text }]}>{note.text}</Text><Text style={[styles.publicNoteMeta, { color: colors.violet }]}>{note._count.applauds} applause · {note._count.comments} comments</Text></View>)}</View>}
    </View>
    <Modal transparent visible={charterDialogOpen} animationType="fade" onRequestClose={() => !sending && setCharterDialogOpen(false)}><View style={styles.backdrop}><View style={[styles.charterDialog, { backgroundColor: colors.surface }]}><Text style={styles.charterDialogEyebrow}>DIRECT DELIVERY</Text><Text style={[styles.charterDialogTitle, { color: colors.navy }]}>{charterSent ? "Charter Plane sent" : "Send Charter Plane?"}</Text><Text style={[styles.charterDialogCopy, { color: colors.muted }]}>{`A red Charter Plane will be sent directly to ${profile.anonymousUsername}'s desk. It costs 100 Paisa (10× a regular plane) and remains there for 24 hours.`}</Text>{!charterSent && <><Text style={[styles.messageLabel, { color: colors.text }]}>Your message</Text><TextInput value={charterMessage} onChangeText={setCharterMessage} maxLength={160} multiline placeholder="Write a short note for this person" placeholderTextColor={colors.muted} style={[styles.messageInput, { borderColor: colors.border, color: colors.text }]} /><Text style={[styles.messageCount, { color: colors.muted }]}>{charterMessage.length}/160</Text></>}{charterNotice && <Text style={[styles.charterNotice, { color: charterSent ? colors.teal : colors.danger }]}>{charterNotice}</Text>}<View style={styles.charterDialogActions}>{!charterSent && <TouchableOpacity disabled={sending} onPress={() => setCharterDialogOpen(false)} style={[styles.dialogCancel, { borderColor: colors.border }]}><Text style={[styles.dialogCancelText, { color: colors.muted }]}>Cancel</Text></TouchableOpacity>}<TouchableOpacity disabled={sending || (!charterSent && !charterMessage.trim())} onPress={() => charterSent ? setCharterDialogOpen(false) : send()} style={[styles.dialogSend, (sending || (!charterSent && !charterMessage.trim())) && styles.disabled]}><Text style={styles.dialogSendText}>{charterSent ? "Done" : sending ? "Sending…" : "Send for 100 Paisa"}</Text></TouchableOpacity></View></View></View></Modal>
  </ScrollView></SafeAreaView>;
}

const styles = StyleSheet.create({
  safe: { flex: 1 }, content: { padding: 22, paddingBottom: 42 }, back: { fontWeight: "800", fontSize: 14 }, loader: { marginTop: 80 }, card: { marginTop: 26, padding: 19, borderRadius: 22, borderWidth: 1 }, identity: { flexDirection: "row", alignItems: "center" }, avatar: { height: 58, width: 58, borderRadius: 18, alignItems: "center", justifyContent: "center" }, initial: { fontSize: 25, fontWeight: "900" }, identityCopy: { marginLeft: 13, flex: 1 }, name: { fontSize: 22, fontWeight: "900" }, memberSince: { fontSize: 12, marginTop: 4 }, limited: { borderRadius: 12, padding: 11, marginTop: 16 }, limitedText: { fontSize: 12, lineHeight: 18, textAlign: "center", fontWeight: "700" }, photoSection: { marginTop: 20 }, sectionLabel: { fontSize: 10, letterSpacing: 1.1, fontWeight: "900" }, photoFrame: { alignSelf: "stretch", aspectRatio: 0.94, marginTop: 9, padding: 7, borderWidth: 4, borderRadius: 6, backgroundColor: "#5D3C2D", shadowOpacity: 0.26, shadowRadius: 6, elevation: 4, overflow: "hidden" }, photoMat: { flex: 1, padding: 5, backgroundColor: "#FFF6E9" }, photoStage: { flex: 1, backgroundColor: "#E8DED3", alignItems: "center", justifyContent: "center" }, photoImage: { width: "100%", height: "100%" }, photoHint: { position: "absolute", bottom: 11, alignSelf: "center", backgroundColor: "rgba(47, 31, 23, .78)", borderRadius: 999, paddingHorizontal: 10, paddingVertical: 5 }, photoHintText: { color: "#FFF9F0", fontSize: 10, fontWeight: "900" }, photoStatus: { fontSize: 11, lineHeight: 17, fontWeight: "700", textAlign: "center", marginTop: 10 }, photoUnavailable: { height: 220, borderWidth: 1, borderRadius: 14, borderStyle: "dashed", marginTop: 9, alignItems: "center", justifyContent: "center", padding: 22 }, unavailableIcon: { fontSize: 28, fontWeight: "900" }, unavailableTitle: { fontSize: 14, fontWeight: "900", textAlign: "center", marginTop: 9 }, unavailableText: { fontSize: 12, lineHeight: 18, textAlign: "center", marginTop: 5 }, detailsPanel: { alignSelf: "stretch", borderWidth: 1, borderRadius: 14, padding: 14, marginTop: 22 }, bio: { fontSize: 15, lineHeight: 22, marginTop: 9, fontWeight: "600" }, tags: { flexDirection: "row", gap: 8, marginTop: 13, flexWrap: "wrap" }, tag: { borderRadius: 999, paddingVertical: 6, paddingHorizontal: 12 }, tagText: { fontWeight: "800", fontSize: 12 }, emptyDetails: { fontSize: 13, lineHeight: 19, marginTop: 9 }, linkButton: { alignSelf: "stretch", borderWidth: 1, borderRadius: 12, padding: 13, marginTop: 14, alignItems: "center" }, linkText: { fontWeight: "800" }, connectButton: { alignSelf: "stretch", padding: 14, alignItems: "center", borderRadius: 12, marginTop: 14 }, connectText: { color: "#FFF", fontWeight: "900" }, reportMember: { alignSelf: "center", padding: 11, marginTop: 6 }, reportMemberText: { fontSize: 12, fontWeight: "900" }, disabled: { opacity: .55 }, notesSection: { alignSelf: "stretch", borderTopWidth: 1, marginTop: 20, paddingTop: 17 }, notesTitle: { fontSize: 15, fontWeight: "900", alignSelf: "flex-start" }, publicNote: { alignSelf: "stretch", borderRadius: 9, padding: 11, marginTop: 10 }, publicNoteText: { fontSize: 13, lineHeight: 19, fontWeight: "700" }, publicNoteMeta: { fontSize: 10, fontWeight: "800", marginTop: 7 },
  charterButton: { backgroundColor: "#B8443F" }, charterHelp: { color: "#FFF0E8", fontSize: 10, fontWeight: "700", marginTop: 4 }, backdrop: { flex: 1, justifyContent: "center", padding: 24, backgroundColor: "rgba(42, 28, 21, .62)" }, charterDialog: { borderRadius: 20, padding: 22 }, charterDialogEyebrow: { color: "#B8443F", fontSize: 10, letterSpacing: 1.2, fontWeight: "900" }, charterDialogTitle: { fontSize: 22, fontWeight: "900", marginTop: 7 }, charterDialogCopy: { fontSize: 14, lineHeight: 21, marginTop: 10 }, messageLabel: { fontWeight: "900", fontSize: 12, marginTop: 16 }, messageInput: { borderWidth: 1, borderRadius: 12, padding: 11, minHeight: 82, marginTop: 7, fontSize: 14, textAlignVertical: "top" }, messageCount: { fontSize: 10, fontWeight: "800", textAlign: "right", marginTop: 4 }, charterNotice: { fontSize: 12, lineHeight: 18, fontWeight: "700", marginTop: 14 }, charterDialogActions: { flexDirection: "row", gap: 10, marginTop: 22 }, dialogCancel: { flex: 1, minHeight: 46, borderWidth: 1, borderRadius: 12, alignItems: "center", justifyContent: "center" }, dialogCancelText: { fontWeight: "800", fontSize: 12 }, dialogSend: { flex: 1, minHeight: 46, borderRadius: 12, backgroundColor: "#B8443F", alignItems: "center", justifyContent: "center", paddingHorizontal: 8 }, dialogSendText: { color: "#FFF", fontWeight: "900", fontSize: 12 },
});
