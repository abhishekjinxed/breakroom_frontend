import { useCallback, useEffect, useState } from "react";
import { ActivityIndicator, SafeAreaView, ScrollView, StyleSheet, Text, TextInput, TouchableOpacity, View } from "react-native";
import { router } from "expo-router";
import { getMembers, PublicProfile } from "../api/users";
import { useAuth } from "../context/AuthContext";
import { useTheme } from "../context/ThemeContext";
export default function Directory() { const { token } = useAuth(); const { colors } = useTheme(); const [query, setQuery] = useState(""); const [members, setMembers] = useState<PublicProfile[]>([]); const [loading, setLoading] = useState(true); const load = useCallback(async () => { if (!token) return; try { setMembers(await getMembers(token, query)); } finally { setLoading(false); } }, [token, query]); useEffect(() => { const timer = setTimeout(load, 250); return () => clearTimeout(timer); }, [load]); return <SafeAreaView style={[styles.safe, { backgroundColor: colors.canvas }]}><ScrollView contentContainerStyle={styles.content}><Text style={[styles.eyebrow, { color: colors.teal }]}>MEMBER DIRECTORY</Text><Text style={[styles.title, { color: colors.navy }]}>Find people to connect with</Text><TextInput value={query} onChangeText={setQuery} placeholder="Search members" placeholderTextColor={colors.muted} style={[styles.input, { color: colors.text, borderColor: colors.border }]} />{loading ? <ActivityIndicator color={colors.teal} style={styles.loader} /> : members.map((member) => <TouchableOpacity key={member.id} onPress={() => router.push(`/profile/${member.id}` as any)} style={[styles.row, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={[styles.avatar, { backgroundColor: colors.violetSoft }]}><Text style={[styles.initial, { color: colors.violet }]}>{member.anonymousUsername[0]?.toUpperCase()}</Text></View><View><Text style={[styles.name, { color: colors.text }]}>{member.anonymousUsername}</Text><Text style={[styles.detail, { color: colors.muted }]}>{[member.age !== null ? `Age ${member.age}` : null, member.gender].filter(Boolean).join(" · ") || "View profile"}</Text></View></TouchableOpacity>)}</ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1 }, content: { padding: 22, paddingBottom: 36 }, eyebrow: { fontSize: 11, fontWeight: "900", letterSpacing: 1.3 }, title: { fontSize: 28, lineHeight: 35, fontWeight: "900", marginTop: 7 }, input: { borderWidth: 1, borderRadius: 12, padding: 13, marginTop: 19 }, loader: { marginTop: 45 }, row: { marginTop: 11, borderWidth: 1, borderRadius: 15, padding: 14, flexDirection: "row", alignItems: "center", gap: 12 }, avatar: { height: 44, width: 44, borderRadius: 14, alignItems: "center", justifyContent: "center" }, initial: { fontSize: 18, fontWeight: "900" }, name: { fontWeight: "900", fontSize: 15 }, detail: { fontSize: 12, marginTop: 4 } });
